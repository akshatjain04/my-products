# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}/api/products"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "ProductController_createProduct_5b0158b3eb_Test.csv"
    fields:
      - "productName"
      - "productDescription"
      - "productPrice"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post: 
          url: "/"
          json:
            name: "{{ productName }}"
            description: "{{ productDescription }}"
            price: "{{ productPrice }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"

  - name: "API request with validation failure"
    flow:
      - post: 
          url: "/invalid-url"
          json:
            name: "{{ productName }}"
            description: "{{ productDescription }}"
            price: "{{ productPrice }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post: 
          url: "/"
          json:
            name: "{{ productName }}"
            description: "{{ productDescription }}"
            price: "{{ productPrice }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
