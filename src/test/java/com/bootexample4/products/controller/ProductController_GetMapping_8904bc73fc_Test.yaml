# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}/api/products"
  plugins:
    expect: {}
  payload:
    path: "ProductController_GetMapping_8904bc73fc_Test.csv"
    fields: ["id"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get: 
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "product"

  - name: "API request with validation failure"
    flow:
      - get: 
          url: "/invalidPath"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get: 
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "product"
            - equals: 
                product: "Expected Product Data"
