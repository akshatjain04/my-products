# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}/api/products"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "ProductController_getAllProducts_e267ceea76_Test.csv"
    fields: ["id", "name", "price"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get: "/"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "price"

  - name: "API request with validation failure"
    flow:
      - get: "/invalid-url"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get: "/?name=special%20characters%20or%20large%20data"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "name"
          - hasProperty: "price"
