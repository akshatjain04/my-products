# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# ROOST_METHOD_HASH=75f959ae6e
# ROOST_METHOD_SIG_HASH=715fb1aa53
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Integration Tests for ProductController API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Delete a product using DELETE method',
#     steps: "Given the API base URL 'http://localhost:8080'\n" +
#       "And an existing product ID '1'\n" +
#       "When I send a DELETE request to '/products/1'\n" +
#       'Then the response status should be 200\n' +
#       'And the response body should be empty',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080/products"
  phases:
    - duration: 30
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Scenario: Delete a product using DELETE method"
    flow:
      - log: "Sending DELETE request to delete an existing product with ID 1 and expecting response status 200."
      - delete:
          url: "/1"
          headers:
            name: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasProperty: "message"
            - headerEquals:
                Content-Type: "application/json"
            - jmespath:
                message: "Product deleted successfully."
      - log: "Expected response body to contain confirmation of successful deletion and relevant status."
      - think: 2

payload:
  path: "DeleteProductUsingDeleteMethod.csv"
  fields:
    - productId
  skipHeader: true
  delimiter: "^|^"
