# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Claude AI and AI Model claude-3-5-haiku-20241022
# 
# ROOST_METHOD_HASH=6d9b6e0e14
# ROOST_METHOD_SIG_HASH=85de03f89e
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Product Update Functionality\n' +
#     '  As a user of the product management system\n' +
#     '  I want to update existing products\n' +
#     '  So that I can modify product information',
#   background: 'Background:\n' +
#     '    Given the Product Management API is running at "http://localhost:8080/products"\n' +
#     '    And the product repository is initialized with test data',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Attempt to update a non-existent product',
#     steps: 'Given no product exists with ID 999\n' +
#       'When I send a PUT request to "/products/999" with the following JSON payload:\n' +
#       '      """\n' +
#       '      {\n' +
#       '        "name": "Phantom Phone",\n' +
#       '        "description": "A non-existent product",\n' +
#       '        "price": 299.99\n' +
#       '      }\n' +
#       'Then the response status code should be 404\n' +
#       'And the response body should be empty',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "http://localhost:8080"
  phases:
    - duration: 1
      arrivalRate: 1
  defaults:
    headers:
      Content-Type: application/json
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Product Update Functionality - Update Non-Existent Product"
    flow:
      - log: "Attempting to update a product that does not exist"
      
      - put:
          url: "/products/999"
          json:
            name: "Phantom Phone"
            description: "A non-existent product"
            price: 299.99
          capture:
            - json: "$"
              as: "requestPayload"
          expect:
            - statusCode: 404
            - contentType: "application/json"
            - bodySize: 0

      - log: "Verified that updating a non-existent product returns a 404 status code"
