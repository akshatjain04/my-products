# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# ROOST_METHOD_HASH=49eda73301
# ROOST_METHOD_SIG_HASH=330fca0660
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Integration Tests for ProductController API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Fetch product by ID using GET method (Valid ID)',
#     steps: "Given the API base URL 'http://localhost:8080'\n" +
#       "And an existing product ID '1'\n" +
#       "When I send a GET request to '/products/1'\n" +
#       'Then the response status should be 200\n' +
#       "And the response body should contain the product with ID '1'",
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: FetchProductByIDUsingGETMethodValidID.csv
    fields:
      - productId
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Fetch product by ID using GET method (Valid ID)
    flow:
      - log: "Sending GET request to fetch product by ID using a valid product ID."
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 2
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasProperty: $.id
      - expect:
          equals:
            jsonpath: "$.id"
            value: "{{ productId }}"
      - expect:
          hasProperty: $.name
      - expect:
          hasProperty: $.price
      - log: "The API response successfully validated. Product fetched by ID has correct properties."
