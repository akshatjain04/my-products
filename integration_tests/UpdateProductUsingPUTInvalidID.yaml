# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# ROOST_METHOD_HASH=a21651d68d
# ROOST_METHOD_SIG_HASH=80dc463ae8
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Integration Tests for ProductController API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Update a product using PUT method (Invalid ID)',
#     steps: "Given the API base URL 'http://localhost:8080'\n" +
#       "And a non-existent product ID '999'\n" +
#       "And the request payload with name 'Updated Laptop', description 'Updated Gaming Laptop', and price '1500.00'\n" +
#       "When I send a PUT request to '/products/999'\n" +
#       'Then the response status should be 404\n' +
#       'And the response body should be empty',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateProductUsingPUTInvalidID.csv"
    fields:
      - name
      - description
      - price
    delimiter: "^|^"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - scenario: Update a product using PUT method (Invalid ID)
    flow:
      - log: "Sending PUT request to update a non-existent product ID with invalid data."
      - put:
          url: "/products/999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
      - expect:
          statusCode: 404
      - expect:
          notHasProperty: "id"
      - expect:
          notHasProperty: "name"
      - expect:
          notHasProperty: "description"
      - expect:
          notHasProperty: "price"
      - expect:
          body: ""
      - log: "Validated that the response status is 404 and the response body is empty."
      - think: 2
