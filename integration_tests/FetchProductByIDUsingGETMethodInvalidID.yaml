# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# ROOST_METHOD_HASH=7d19802d97
# ROOST_METHOD_SIG_HASH=21e79a4815
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Integration Tests for ProductController API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Fetch product by ID using GET method (Invalid ID)',
#     steps: "Given the API base URL 'http://localhost:8080'\n" +
#       "And a non-existent product ID '999'\n" +
#       "When I send a GET request to '/products/999'\n" +
#       'Then the response status should be 404\n' +
#       'And the response body should be empty',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/v2/products"
  plugins:
    expect: {}
  payload:
    path: "FetchProductByIDUsingGETMethodInvalidID.csv"
    fields:
      - productID
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Fetch Product by ID using GET Method: Invalid ID"
    flow:
      - log: "Starting test scenario to fetch a product by invalid ID."
      - get:
          url: "/{{ productID }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - log: "GET request sent to '/products/{{ productID }}' with an invalid ID."
      - expect:
          - statusCode: 404
          - body: ""
          - notHasProperty: "productDetails"
          - hasHeader:
              name: "Content-Type"
          - headerEquals:
              name: "Content-Type"
              value: "application/json"
      - log: "Verified response - Status Code 404, Empty body, No 'productDetails', and proper Content-Type header."
      - think: 2
