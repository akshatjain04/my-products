# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework

# RoostTestHash=49b35fa9c1


# Artillery test script in YAML format for successful API requests

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - flow:
      - get:
          url: /recipients/branches/{id}/registrations
          payload:
            id: {{ $processEnvironment.BRANCH_ID }}
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - registrations
              - total
            hasHeader:
              - api-version
              - page-size
              - page-offset
      - think: 500
