# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/v3/shapes"
          headers:
            Approov-Token: "adasdasdsadasd"
          expect:
            - statusCode: 200
            - hasProperty: "shapes"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/v3/shapes"
          headers:
            Approov-Token: "wrong-token"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/v3/shapes"
          headers:
            Approov-Token: "adasdasdsadasd"
          json:
            shape: "very large shape data or special characters"
          expect:
            - statusCode: "{{ 200..599 }}"
            - hasProperty: "shapes"
