# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /login_post for http method type POST in artillery framework

# RoostTestHash=e0cf5b2e0e


config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    include: ["expect.js"]
  security:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
  payload:
    login_post_success.csv:
      skipHeader: true
      fields:
        - email
        - otp
        - client
  variables:
    token: {{$processEnvironment.AUTH_TOKEN}}
scenarios:
  - flow:
      - get:
          url: /login
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
          log: "GET /login: API version header present"
      - think: 500
      - post:
          url: /login
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          body:
            email: {{$csv.email}}
            otp: {{$csv.otp}}
            client: {{$csv.client}}
          expect:
            statusCode: 201
            contentType: application/json
            hasProperty: token
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
          log: "POST /login: Token received"
      - think: 1000
