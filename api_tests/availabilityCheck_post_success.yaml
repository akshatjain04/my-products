# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=2445a4c161
# 
# 

# ********RoostGPT********
config:
  target: "TARGET_URL"
  plugins:
    expect: {}
  environments:
    production:
      target: "TARGET_URL"
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "customer.subscriberId"
      - "address.id"
      - "address.complement.complements"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Availability Check API Success Response Scenario"
    flow:
      - log: "Starting availabilityCheck API test"
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer AUTH_TOKEN"
          json:
            customer:
              subscriberId: "{{ customer.subscriberId }}"
            address:
              id: "{{ address.id }}"
              complement:
                complements: "{{ address.complement.complements }}"
      - think: 1
      - expect:
          - statusCode: 200
      - expect:
          - hasProperty: "control.type"
      - expect:
          - hasProperty: "control.code"
      - expect:
          - hasProperty: "control.message"
      - expect:
          - hasProperty: "resource.inventoryId"
      - expect:
          - hasProperty: "resource.maxBandwidth"
      - expect:
          - hasProperty: "resource.availabilityType"
      - expect:
          - hasProperty: "resource.availabilityCode"
      - expect:
          - hasProperty: "resource.availabilityDescription"
      - expect:
          - hasProperty: "resource.products.product"
      - expect:
          - headers:
              - hasHeader: "Content-Type"
