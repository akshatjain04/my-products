# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=a251be39a4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_tresourcePoolManagement_(2)_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "id"
      - "type"
      - "value"
    skipHeader: true
    delimiter: "^|^"
  
scenarios:
  - name: "Availability Check API Test"
  
    flow:
      - log: "Posting the request to /availabilityCheck"
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            {{API_SPECIFICATION.security_scheme_name}}: "{{ $processEnvironment.url1_tresourcePoolManagement_(2)_AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ id }}"
              complement:
                complements:
                  - type: "{{ type }}"
                    value: "{{ value }}"
      - expect:
          - statusCode: 200 
      - expect: 
          - hasProperty: "control.type"
      - equals: 
          - "{{ type }}"
          - "S"
      - expect:
          - hasProperty: "resource.inventoryId"
      - expect:
          - hasProperty: "resource.maxBandwidth"
      - expect:
          - hasProperty: "resource.availabilityType"
      - expect:
          - hasProperty: "resource.availabilityCode"
      - expect:
          - hasProperty: "resource.availabilityDescription"
      - think: 1
