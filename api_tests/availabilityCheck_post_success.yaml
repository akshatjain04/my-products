# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_tresourcePoolManagement_(2)_API_HOST }}"
  plugins:
    expect: {}
  environments:
    production:
      target: "{{ $processEnvironment.url1_tresourcePoolManagement_(2)_API_HOST }}"
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "id"
      - "type"
      - "value"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Availability Check API Success Response Scenario"
    flow:
      - log: "Starting availabilityCheck API test"
      - post:
         url: "/availabilityCheck"
         headers:
           Content-Type: "application/json"
           Authorization: "Bearer {{$processEnvironment.url1_tresourcePoolManagement_(2)_AUTH_TOKEN}}"
         json:
           subscriberId: "{{ subscriberId }}"
           id: "{{ id }}"
           type: "{{ type }}"
           value: "{{ value }}"
      - think: 1
      - expect:
          statusCode: 200
      - expect:
          hasProperty: "resource.inventoryId"
      - expect:
          hasProperty: "resource.maxBandwidth"
      - expect:
          hasProperty: "resource.availabilityType"
      - expect:
          hasProperty: "resource.availabilityCode"
      - expect:
          hasProperty: "control.type"
      - expect:
          hasProperty: "control.code"
      - expect:
          hasProperty: "product.type"
      - expect:
          hasProperty: "product.catalogId"
      - expect:
          hasProperty: "product.name"
      - expect:
          hasProperty: "product.technology"
      - expect:
          headers:
            - hasHeader: "Content-Type"
