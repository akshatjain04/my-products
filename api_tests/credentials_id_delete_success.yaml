# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_API_HOST }}"
  plugins:
    expect: {}
  ensure:
    - statusCode: 200
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Testing delete endpoint for successful operation"
    flow:
      - log: "Starting API test scenario to validate correct delete endpoint behavior with valid input payloads"
      - post:
          url: "/credentials/{{id}}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
          - hasHeader: "api-version"
          - equals:
              - "api-version"
              - "1.0.0"
      - log: "Delete request succeeded"
      - think: 5
