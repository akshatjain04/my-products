# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://postman-echo.com"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "get_GET_1690350573.csv"
    fields:
      - "source"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/get?source={{ source }}"
          expect:
            - statusCode: 200
            - hasProperty: ["args", "source"]
            - equals: ["{{ source }}", "newman-sample-github-collection"]

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/get?source="
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/get?source={{ source }}"
          expect:
            - statusCode: 200
            - hasProperty: ["args", "source"]

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/get?source={{ source }}"
          expect:
            - statusCode: 200
            - hasProperty: ["args", "source"]
            - equals: ["{{ source }}", "newman-sample-github-collection"]

  - name: "API request with authorization"
    flow:
      - get:
          url: "/get?source={{ source }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: ["args", "source"]
            - equals: ["{{ source }}", "newman-sample-github-collection"]
