# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=f1fc1d06f7

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: credentials_id_delete_others.csv
    skipHeader: true
scenarios:
  - scenario: Failure Test Cases
    flow:
      - log: Sending Delete Request with Invalid Credential ID
        delete:
          url: /credentials/{{ invalid_id }}
          security:
            ApiKeyAuth:
              Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              - error
              - description
            hasHeader:
              - api-version
      - think: 500ms
      - log: Sending Delete Request without Credential ID
        delete:
          url: /credentials/
          security:
            ApiKeyAuth:
              Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              - error
              - description
            hasHeader:
              - api-version
      - think: 500ms
      - log: Sending Delete Request with Incorrect URL
        delete:
          url: /incorrect/url
          security:
            ApiKeyAuth:
              Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 404
      - think: 500ms
      - log: Sending Delete Request without Authorization Header
        delete:
          url: /credentials/{{ credentials_id }}
          expect:
            statusCode: 401
            contentType: application/json
            hasProperty:
              - error
              - description
            hasHeader:
              - api-version
      - think: 500ms
