# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /listener/productOrderCreateEvent_post for http method type POST in artillery framework
# RoostTestHash=9b45f7ab6a
# 
# 

# ********RoostGPT********

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_productOrderCreateEvent_post_others.csv"
    fields:
      - "date"
      - "id"
      - "associatedDocument"
      - "correlationOrder"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Test scenario for non-success responses
    flow:
      - log: "Sending request with missing date field in the payload"
      - post: 
          url: "/listener/productOrderCreateEvent"
          headers: 
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json: 
            order:
              id: "{{ id }}"
              associatedDocument: "{{ associatedDocument }}"
              correlationOrder: "{{ correlationOrder }}"
          expect: 
            - statusCode: 400
      - think: 5

      - log: "Sending request with invalid URL"
      - post: 
          url: "/listener/invalidURL"
          headers: 
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json: 
            date: "{{ date }}"
            order:
              id: "{{ id }}"
              associatedDocument: "{{ associatedDocument }}"
              correlationOrder: "{{ correlationOrder }}"
          expect: 
            - statusCode: 404
      - think: 5

      - log: "Sending unauthorized request"
      - post: 
          url: "/listener/productOrderCreateEvent"
          headers: 
            authorization: "Invalid token"
          json: 
            date: "{{ date }}"
            order:
              id: "{{ id }}"
              associatedDocument: "{{ associatedDocument }}"
              correlationOrder: "{{ correlationOrder }}"
          expect: 
            - statusCode: 401
            - hasHeader: "www-authenticate"
      - think: 5
