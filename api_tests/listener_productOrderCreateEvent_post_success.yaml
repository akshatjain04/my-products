# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /listener/productOrderCreateEvent_post for http method type POST in artillery framework
# RoostTestHash=9b45f7ab6a
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_productOrderCreateEvent_post_success.csv"
    fields: 
      - "date"
      - "id"
      - "associatedDocument"
      - "correlationOrder"
    delimiter: "^|^"
  environments:
    local: 
      target: "{{ $processEnvironment.API_HOST }}"
      variables: 
        token: "{{$processEnvironment.AUTH_TOKEN}}"
      
scenarios:
  - name: "Product order create event API test"
    flow:
      - log: "Starting product order create event API test"
      - post:
          url: "/listener/productOrderCreateEvent"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{ token }}"
          json:
            date: "{{ date }}"
            order: 
              id: "{{ id }}"
              associatedDocument: "{{ associatedDocument }}"
              correlationOrder: "{{ correlationOrder }}"
      - expect:
            - statusCode: 204
            - contentType: "{{ $processEnvironment.CONTENT_TYPE }}"
      - think: 1
      - capture:
            json: "$.data.id"
            as: "id"
      - log: "End of product order create event API test"
