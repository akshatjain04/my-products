# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=28a54f1579

--- Corrected Artillery Test Case Start ---

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

payload:
  apikeys_post_others.csv:
    skipHeader: true
    fields:
      - name
      - email
      - password
      - type

scenarios:
  - scenario:
      - log:
          message: "Create API Key with Invalid Email Format"
      - post:
          url: /apikeys
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: invalid_email_format
            password: password
            name: random_name
            type: 4
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              description: Descriptive Error Message
              error: validation/email-pattern
      - think: 500ms
      - log:
          message: "Create API Key with Invalid Password Format"
      - post:
          url: /apikeys
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: valid_email@example.com
            password: short
            name: random_name
            type: 4
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              description: Descriptive Error Message
              error: validation/password-length
      - think: 500ms
      - log:
          message: "Create API Key with Invalid Name Format"
      - post:
          url: /apikeys
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: valid_email@example.com
            password: valid_password
            name: 12345
            type: 4
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              description: Descriptive Error Message
              error: validation/name-pattern
      - think: 500ms
      - log:
          message: "Create API Key with Invalid Type"
      - post:
          url: /apikeys
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: valid_email@example.com
            password: valid_password
            name: random_name
            type: invalid_type
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              description: Descriptive Error Message
              error: validation/type-mismatch
      - think: 500ms
      - log:
          message: "Create API Key with Missing Required Fields"
      - post:
          url: /apikeys
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: valid_email@example.com
          expect:
            statusCode: 400
            contentType: application/json
            hasProperty:
              description: Descriptive Error Message
              error: validation/missing-required-field
      - think: 500ms
      - log:
          message: "Create API Key with Invalid URL"
      - post:
          url: /invalid/url
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            email: valid_email@example.com
            password: valid_password
            name: random_name
            type: 4
          expect:
            notStatusCode:
              - 200
              - 201

--- Corrected Artillery Test Case End ---
