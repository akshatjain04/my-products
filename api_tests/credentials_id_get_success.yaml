# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url2_credentialregister-wallet-1.0.0-resolved_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_success.csv"
    fields: ['$id']
    skipHeader: true
    delimiter: "^|^"
    
scenarios:
  - name: Test GET credentials API Endpoint
    flow:
      - get:
          url: "/credentials/{{ $id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.url2_credentialregister-wallet-1.0.0-resolved_AUTH_TOKEN}}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
      - soHeader: "api-version"
        equals: "1.0.0"
      - hasProperty: 'id'
      - hasProperty: 'format'
      - hasProperty: 'credential'
      - hasProperty: 'status'
      - hasProperty: 'statusUpdatedAt'
      - hasProperty: 'createdAt'
      - soProperty: "id"
        matchesRegexp: "^[0-9a-f]{8}-[0-9a-f]{4}-[4][0-9a-f]{3}-[89AB][0-9a-f]{3}-[0-9a-f]{12}$"
      - soProperty: "status"
        equals: "active"
      - think: 1
