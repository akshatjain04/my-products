# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=49b35fa9c1

config:
  target: {{ $processEnvironment.API_HOST }}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    csv: registrations_get_others.csv
    skipHeader: true
    id: {{ $csv.id }}
    name: {{ $csv.name }}
    description: {{ $csv.description }}
    credential_url: {{ $csv.credential_url }}
  expect:
    enabled: true
security_schema:
  ApiKeyAuth:
    type: apiKey
    name: Token
    in: header
scenarios:
  - name: Failure Scenario
    flow:
      - get:
          description: Get Registrations with incorrect URL
          url: /recipients/branches/{{ id }}/registrations/invalid
          expect:
            statusCode: 404
      - get:
          description: Get Registrations without ID
          url: /recipients/branches//registrations
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with invalid ID
          url: /recipients/branches/invalid/registrations
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with invalid credential_url
          url: /recipients/branches/{{ id }}/registrations?credential_url=invalid
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with invalid size
          url: /recipients/branches/{{ id }}/registrations?size=invalid
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with invalid offset
          url: /recipients/branches/{{ id }}/registrations?offset=invalid
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with negative size
          url: /recipients/branches/{{ id }}/registrations?size=-1
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with negative offset
          url: /recipients/branches/{{ id }}/registrations?offset=-1
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with size greater than maximum
          url: /recipients/branches/{{ id }}/registrations?size=51
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with invalid query parameters
          url: /recipients/branches/{{ id }}/registrations?invalid_param=invalid
          expect:
            statusCode: 400
      - get:
          description: Get Registrations with no authorization
          url: /recipients/branches/{{ id }}/registrations
          expect:
            statusCode: 401
      - think: 500
