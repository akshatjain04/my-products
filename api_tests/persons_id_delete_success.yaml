# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=819791060f


config:
  target: {{ $processEnvironment.API_HOST }}
  plugins:
    expect: enable
payload:
  csv: persons_id_delete_success.csv
  skipHeader: true
scenarios:
  - flow:
      - get:
          url: /recipients/persons/{{ persons_id }}
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: api-version
            hasProperty:
              - id
              - email
              - name
              - phone
      - think: 500
      - delete:
          url: /recipients/persons/{{ persons_id }}
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 204
      - think: 500
