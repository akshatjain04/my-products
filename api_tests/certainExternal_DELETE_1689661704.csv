import csv
import faker
import random

# Initialize Faker library
fake = faker.Faker()

# Define data fields
data_fields = ["id", "first_name", "last_name", "email", "date_of_birth", "phone_number"]

# Open or create CSV file
with open('dummy_data.csv', mode='w', newline='') as file:
    writer = csv.writer(file)
    writer.writerow(data_fields)  # Write column headers

    # Generate two rows of dummy data
    for _ in range(2):
        id = random.randint(1000, 9999)
        first_name = fake.first_name()
        last_name = fake.last_name()
        email = fake.email()
        date_of_birth = fake.date_of_birth(minimum_age=22, maximum_age=99)
        phone_number = fake.phone_number()

        # Write data row
        writer.writerow([id, first_name, last_name, email, date_of_birth, phone_number])
