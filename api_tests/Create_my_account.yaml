# Test generated by RoostGPT for test MiniProjects using AI Type Vertex AI and AI Model code-bison-32k


# Artillery test script generated from Gherkin scenario:
# Scenario - {"background":null,"rule":null,"scenario":"Create my account \r\n    When the client sends a POST request \"/accounts\" with the accounts_body payload\r\n    Then create an account with the specified informatio\r\n    And verify the account created using GET request for \"/me\"","title":"Create my account"}

config:
  target: "{{ $processEnvironment.API_HOST }}/accounts"
  expect:
    status: 201
  payload:
    - name: accounts_body
      file: Create_my_account.csv
      skipHeader: true

scenarios:
  - scenario: Create my account
    flow:
      - log: Sending POST request to /accounts
      - post:
          headers:
            Content-Type: application/json
          json: ${accounts_body}
      - log: Verifying account creation
      - get:
          target: "{{ $processEnvironment.API_HOST }}/me"
          headers:
            Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
      - think: 500
