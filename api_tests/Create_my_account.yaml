# Test generated by RoostGPT for test MiniProjects using AI Type Vertex AI and AI Model code-bison-32k


# Artillery test script for API testing
config:
  target: "{{ $processEnvironment.API_HOST }}/accounts"
  expect:
    status: 201
  payload:
    - name: "John Doe"
    - email: "john.doe@example.com"
    - password: "securepassword"
    - phone: "0123456789"
    - country: "NL"
    - language: "en"
    - currency: "EUR"
    - timezone: "Europe/Amsterdam"
  skipHeader: true

scenarios:
  - scenario: Create my account
    flow:
      - log: Sending POST request to /accounts
      - post:
          url: /accounts
          headers:
            Content-Type: application/json
          json: ${accounts_body}
      - log: Verifying account creation
      - get:
          url: /me
          headers:
            Authorization: Bearer {{ $processEnvironment.AUTH_TOKEN }}
      - think: 500
