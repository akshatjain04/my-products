// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Scenario: Get all products"
    # This scenario tests the GET /api/products endpoint to verify that it returns an empty list of products
    flow:
      - get:
          url: "/api/products"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: []
            - equals: []
            - contentType: "application/json"
            - responseTime: "<=200"
      - log: "Get all products request sent successfully and the list of products returned is empty as expected"
