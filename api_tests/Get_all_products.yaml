// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - name: "Scenario: Get all products"
    # This scenario is expected to return an empty list of products
    # When the client sends a GET request "/api/products" to get the list of all products
    # Then the list of products returned should be empty
    flow:
      - get:
          url: "/api/products"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - equals:
                json: "$.length"
                value: 0
