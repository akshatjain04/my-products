# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid and failure test cases for /products API"
    flow:
      - log: "Sending request with invalid URL to test error handling"
      - get:
          url: "/invalid/products"
          expect:
            - statusCode: 404
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1

      - log: "Sending request with invalid Authorization token to test security"
      - get:
          url: "/products"
          expect:
            - statusCode: 401
          headers:
            Authorization: "Bearer invalidtoken"
      - think: 1

      - log: "Sending request with missing required fields in payload to test validation"
      - post:
          url: "/products"
          json:
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1

      - log: "Sending request to test response header presence"
      - get:
          url: "/products"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
