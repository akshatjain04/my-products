# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_others.csv"
    fields:
      - "invalidId"
      - "invalidName"
      - "invalidDescription"
      - "invalidPrice"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Negative test scenarios for /products API"
    flow:
      - log: "Sending a request with an invalid URL to check the API response"
      - get:
          url: "/invalidProducts"
          expect:
            - statusCode: 404
            - hasProperty: "error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 2

      - log: "Sending a request with an invalid Authorization token to check the API response"
      - get:
          url: "/products"
          expect:
            - statusCode: 401
            - hasProperty: "error"
          headers:
            Authorization: "Bearer invalidToken"
      - think: 2

      - log: "Sending a request with missing required fields in the payload"
      - get:
          url: "/products?id={{ invalidId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 2

      - log: "Sending a request with invalid data types for fields in the payload"
      - get:
          url: "/products?name={{ invalidName }}&description={{ invalidDescription }}&price={{ invalidPrice }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 2

      - log: "Sending a request to check if the API is handling unexpected large payloads"
      - get:
          url: "/products?name=VeryLongStringToTestTheLimitOfTheFieldAndSeeHowTheAPIHandlesIt"
          expect:
            - statusCode: 413
            - hasProperty: "error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
