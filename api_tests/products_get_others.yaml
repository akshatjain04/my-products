# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Negative test cases for /products API"
    flow:
      - log: "Sending request with invalid URL"
      - get:
          url: "/invalid/products"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending request with missing required fields in payload"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: ""
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending request with invalid authorization token"
      - get:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending request with invalid content type"
      - post:
          url: "/products"
          headers:
            Content-Type: "text/plain"
          body: "Invalid content type"
          expect:
            - statusCode: 415
      - think: 1

      - log: "Sending request with invalid method type"
      - put:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
      - think: 1

      - log: "Sending request with invalid payload data type"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: 123
            description: true
            price: "free"
          expect:
            - statusCode: 422
      - think: 1

      - log: "Sending request with extra fields in payload"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            id: 999
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
            extra_field: "extra_value"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending request with invalid price range"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: -10
          expect:
            - statusCode: 400
