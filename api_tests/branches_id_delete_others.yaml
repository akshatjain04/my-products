# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=1ce4bf6e3c

# Artillery YAML Test Script for Non-Success API Responses

---
config:
  target: {{ $processEnvironment.API_HOST }}
  phases:
    - duration: 60
      arrivalRate: 10
  processor: expect
  securitySchemes:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'

scenarios:
  - scenario: Failure-Inducing Requests
    - get:
        url: /recipients/branches/invalid-uuid
        expect:
          statusCode: 400
          contentType: application/json
          hasProperty: error
          hasProperty: description
          headerEquals: api-version: 1.0.0
          matchesRegexp: "error/.*$"
    - post:
        url: /recipients/branches
        headers:
          Token: {{$processEnvironment.AUTH_TOKEN}}
        payload:
          csv: branches_id_delete_others.csv
          skipHeader: true
        expect:
          statusCode: 400
          contentType: application/json
          hasProperty: error
          hasProperty: description
          hasProperty: value
          hasProperty: field
          hasProperty: schema_field
          headerEquals: api-version: 1.0.0
          matchesRegexp: "validation/.*$"
    - patch:
        url: /recipients/branches/invalid-uuid
        headers:
          Token: {{$processEnvironment.AUTH_TOKEN}}
        payload:
          csv: branches_id_delete_others.csv
          skipHeader: true
        expect:
          statusCode: 400
          contentType: application/json
          hasProperty: error
          hasProperty: description
          hasProperty: value
          hasProperty: field
          hasProperty: schema_field
          headerEquals: api-version: 1.0.0
          matchesRegexp: "validation/.*$"
    - delete:
        url: /recipients/branches/invalid-uuid
        headers:
          Token: {{$processEnvironment.AUTH_TOKEN}}
        expect:
          statusCode: 400
          contentType: application/json
          hasProperty: error
          hasProperty: description
          headerEquals: api-version: 1.0.0
          matchesRegexp: "validation/.*$"
    - think: 500
