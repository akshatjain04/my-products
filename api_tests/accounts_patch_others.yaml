# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=2758affb7e

---
# Artillery test script in YAML format focusing on non-success API responses

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true # Enable 'expect' plugin

scenarios:
  - scenario: Failure-Inducing Requests
    flow:
      - get:
          url: /accounts/1234567890 # Invalid account ID
          log: "GET request with invalid account ID"
          expect:
            - statusCode: 404
            - notHasHeader: content-type
            - hasProperty: message
            - hasProperty: error
      - post:
          url: /accounts
          log: "POST request with invalid payload"
          payload:
            email: "invalid@email"
          expect:
            - statusCode: 400
            - contentType: application/json
            - hasProperty: error
            - hasProperty: message
            - hasProperty: schema_field
            - hasProperty: value
      - patch:
          url: /accounts/1234567890 # Invalid account ID
          log: "PATCH request with invalid payload"
          payload:
            name: "John Doe"
          expect:
            - statusCode: 404
            - notHasHeader: content-type
            - hasProperty: message
            - hasProperty: error
      - delete:
          url: /accounts/1234567890 # Invalid account ID
          log: "DELETE request with invalid account ID"
          expect:
            - statusCode: 404
            - notHasHeader: content-type
            - hasProperty: message
            - hasProperty: error
      - get:
          url: https://invalid-url.com # Invalid URL
          log: "GET request with invalid URL"
          expect:
            - statusCode: 404
            - notHasHeader: content-type
            - hasProperty: message
            - hasProperty: error
      - think: 500 # Wait for 500ms after each request
