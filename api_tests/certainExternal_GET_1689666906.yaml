# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_GET_168966690.csv"
    fields:
      - "accountCode"
      - "profilePin"
  defaults:
    headers:
      Content-Type: "application/json"
  auth:
    type: "basic"
    username: "{{ $processEnvironment.Username }}"
    password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:invalidProfilePin"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:largeProfilePin"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin?param=value"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"

  - name: "API request with authorization"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"
