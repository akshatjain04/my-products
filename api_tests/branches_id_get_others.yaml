# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=061355b093

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    file: branches_id_get_others.csv
    skipHeader: true
  securitySchemes:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
      value: {{$processEnvironment.AUTH_TOKEN}}
scenarios:
  - scenario:
      - log: Logging the start of the failure scenario.
      - get:
          url: /recipients/branches/{{payload.id}}
          authorization:
            ApiKeyAuth:
              value: {{$processEnvironment.AUTH_TOKEN}}
          statusCode: 400
          expect:
            - contentType: application/json
            - hasProperty: error
            - hasProperty: description
            - notHasProperty: schema_field
            - notHasProperty: value
            - notHasProperty: field
      - think: 300
      - log: Logging the end of the failure scenario.
