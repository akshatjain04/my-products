# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true
security:
  ApiKeyAuth:
    - token: {{$processEnvironment.AUTH_TOKEN}}
payload:
  credentials_get_success.csv:
    skipHeader: true
scenarios:
  - flow:
      - get:
          - url: "/credentials"
            headers:
              Token: {{$processEnvironment.AUTH_TOKEN}}
            expect:
              - statusCode: 200
              - contentType: application/json
              - hasProperty: credentials
              - hasProperty: total
              - hasHeader: api-version
              - hasHeader: page-size
              - hasHeader: page-offset
              - jmespath: 'length(credentials) == total'
      - think: 500
