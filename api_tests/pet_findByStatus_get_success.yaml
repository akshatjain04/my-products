# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=44cafec54d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url2_petstore_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "pet_findByStatus_get_success.csv"
    skipHeader: true
    fields:
      - "status"
      - "expectedId"
      - "expectedCategory"
      - "expectedName"
      - "expectedPhotoUrls"
      - "expectedTags"
      - "expectedStatus"

scenarios:
  - name: "Petstore Scenario"
    flow:
      - log: "Initiating GET request to /v2/pet/findByStatus endpoint with valid status"
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            api_key: "{{$processEnvironment.url2_petstore_AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
            - equals: 
                "id" : "{{ expectedId }}"
            - equals: 
                "category" : "{{ expectedCategory }}"
            - equals: 
                "name" : "{{ expectedName }}"
            - equals: 
                "photoUrls" : "{{ expectedPhotoUrls }}"
            - equals: 
                "tags" : "{{ expectedTags }}"
            - equals: 
                "status" : "{{ expectedStatus }}"
      - think: 3
