# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=1ce4bf6e3c


config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  # Test Scenario: Successful API Requests
  - flow:
      # GET /recipients/branches/{id} - Success
      - get:
          url: /recipients/branches/{{ branches_id_get_success }}
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty: api-version

      # POST /recipients/branches - Success
      - post:
          url: /recipients/branches
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: {{ branches_name_post_success }}
            description: {{ branches_description_post_success }}
          expect:
            statusCode: 201
            contentType: application/json
            hasProperty: api-version
            hasProperty: id
            hasProperty: name
            hasProperty: description

      # PATCH /recipients/branches/{id} - Success
      - patch:
          url: /recipients/branches/{{ branches_id_patch_success }}
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            name: {{ branches_name_patch_success }}
            description: {{ branches_description_patch_success }}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty: api-version
            hasProperty: id
            hasProperty: name
            hasProperty: description

      # DELETE /recipients/branches/{id} - Success
      - delete:
          url: /recipients/branches/{{ branches_id_delete_success }}
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 204

      # Think time between requests
      - think: 500
