# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - flow:
      - get:
          url: /recipients/branches
          headers:
            'Token': {{$processEnvironment.AUTH_TOKEN}}
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: total
            - hasProperty: branches
            - branches:
                - hasProperty: id
                - hasProperty: cocNumber
                - hasProperty: cocBranchNumber
                - hasProperty: tradeName
                - hasProperty: tradeNames
                - hasProperty: eoriNumber
                - hasProperty: deregistrationDate
                - hasProperty: createdAt
                - hasProperty: updatedAt
            - total:
                equals: 1

          log:
            message: "GET /recipients/branches: Successful retrieval of branches with status code 200"
      - think: 1000
