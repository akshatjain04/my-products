# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=81332360b2
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.url1_tresourcePoolManagement_(2)_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "availabilityCheck_post_others.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "complementType"
      - "complementValue"
    skipHeader: true
    delimiter: "^|^"
variables:
  auth: "{{ processEnvironment.url1_tresourcePoolManagement_(2)_AUTH_TOKEN }}"
scenarios: 
  - name: "Testing non-success API responses"
    flow:
      - log: 
          expression: "Posting Availability Check with missing subscriberId"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: ""
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - think: 1
      - log:
          expression: "Expect response customer.subscriberId property and 400 as statusCode"
      - expect:
          - hasProperty: ['customer.customer.subscriberId']
          - statusCode: 400
      - think: 1
      - log: 
          expression: "Posting Availability Check with invalid addressId"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "test"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - think: 1
      - log: 
          expression: "Expect response address.id property and 400 as statusCode"
      - expect: 
          - hasProperty: ['address.address.id']
          - statusCode: 400
      - think: 1
      - log: 
          expression: "Access with wrong Auth Token"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "wrongToken"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - think: 1
      - log: 
          expression: "Expect 401 as statusCode for wrong auth token"
      - expect:
          - statusCode: 401
      - think: 1
      - log: 
          expression: "Posting Availability Check to incorrect URL"
      - post:
          url: "/incorrectEndpoint"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - think: 1
      - log: 
          expression: "Expect 404 as statusCode for invalid url"
      - expect:
          - statusCode: 404
      - think: 1
