# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=a251be39a4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_tresourcePoolManagement_(2)_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "availabilityCheck_post_others.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "complementType"
      - "complementValue"
    skipHeader: true
    delimiter: "^|^"
variables:
  auth: { $processEnvironment.url1_tresourcePoolManagement_(2)_AUTH_TOKEN }
scenarios: 
  - name: "Testing non-success API responses"
    flow:
      - log: "Posting Availability Check with missing subscriberId"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: ""
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - expect:
            - statusCode: 400
      - think: 1
      - log: "Posting Availability Check with invalid addressId"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "test"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - expect: 
            - statusCode: 400
      - think: 1
      - log: "Access with wrong Auth Token"
      - post:
          url: "/availabilityCheck"
          headers: 
            authorization: "wrongToken"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - expect:
            - statusCode: 401
      - think: 1
      - log: "Posting Availability Check to incorrect URL"
      - post:
          url: "/incorrectEndpoint"
          headers: 
            authorization: "{{ auth }}"
          json:
            customer: 
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    value: "{{ complementValue }}"
      - expect:
            - statusCode: 404
      - think: 1
