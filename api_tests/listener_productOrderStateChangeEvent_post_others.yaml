# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /listener/productOrderStateChangeEvent_post for http method type POST in artillery framework
# RoostTestHash=6f5fd9fdba
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_productOrderStateChangeEvent_post_others.csv"
    fields:
      - "date"
      - "subscriberId"
      - "id"
      - "description"
      - "type"
      - "code"
      - "associatedDocument"
      - "correlationOrder"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - flow:
    - log: "Create ProductOrderStateChangeEvent with wrong API endpoint."
    - post:
        url: "{{ $processEnvironment.API_HOST }}/wrong/listener/productOrderStateChangeEvent"
        expect: 
          - statusCode: 404
        headers:
          authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          date: "{{ date }}"
          subscriberId: "{{ subscriberId }}"
          id: "{{ id }}"
          description: "{{ description }}"
          type: "{{ type }}"
          code: "{{ code }}"
          associatedDocument: "{{ associatedDocument }}"
          correlationOrder: "{{ correlationOrder }}"
    - think: 1

    - log: "Create ProductOrderStateChangeEvent with wrong API method."
    - post:
        url: "{{ $processEnvironment.API_HOST }}/listener/productOrderStateChangeEvent"
        expect: 
          - statusCode: 405
        headers:
          authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          date: "{{ date }}"
          subscriberId: "{{ subscriberId }}"
          id: "{{ id }}"
          description: "{{ description }}"
          type: "{{ type }}"
          code: "{{ code }}"
          associatedDocument: "{{ associatedDocument }}"
          correlationOrder: "{{ correlationOrder }}"
    - think: 1

    - log: "Create ProductOrderStateChangeEvent without authorization."
    - post:
        url: "{{ $processEnvironment.API_HOST }}/listener/productOrderStateChangeEvent"
        expect: 
          - statusCode: 401
        json:
          date: "{{ date }}"
          subscriberId: "{{ subscriberId }}"
          id: "{{ id }}"
          description: "{{ description }}"
          type: "{{ type }}"
          code: "{{ code }}"
          associatedDocument: "{{ associatedDocument }}"
          correlationOrder: "{{ correlationOrder }}"
    - think: 1

    - log: "Invalid ProductOrderStateChangeEvent - wrong 'code' field."
    - post:
        url: "{{ $processEnvironment.API_HOST }}/listener/productOrderStateChangeEvent"
        expect: 
          - statusCode: 400
        headers:
          authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        json:
          date: "{{ date }}"
          subscriberId: "{{ subscriberId }}"
          id: "{{ id }}"
          description: "{{ description }}"
          type: "{{ type }}"
          code: "999"
          associatedDocument: "{{ associatedDocument }}"
          correlationOrder: "{{ correlationOrder }}"
    - think: 1
