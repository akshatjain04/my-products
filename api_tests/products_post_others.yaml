# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_post_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Negative test cases for creating a product"
    flow:
      - log: "Sending a request with missing required field 'name'"
      - post:
          url: "/products"
          json:
            description: "mehenge chips"
            price: 342
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"

      - think: 1

      - log: "Sending a request with invalid price type"
      - post:
          url: "/products"
          json:
            name: "newchps"
            description: "mehenge chips"
            price: "invalid_price"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"

      - think: 1

      - log: "Sending a request with invalid authorization token"
      - post:
          url: "/products"
          json:
            name: "newchps"
            description: "mehenge chips"
            price: 342
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 405
            - hasProperty: "description"

      - think: 1

      - log: "Sending a request to an invalid URL"
      - post:
          url: "/invalid_url"
          json:
            name: "newchps"
            description: "mehenge chips"
            price: 342
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "description"
