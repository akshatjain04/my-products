# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview


# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_post_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create product with various failure cases"
    flow:
      - log: "Sending a request with missing required field 'name'"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"

      - think: 1

      - log: "Sending a request with invalid URL"
      - post:
          url: "/invalid_products_url"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"

      - think: 1

      - log: "Sending a request with invalid authorization token"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"

      - think: 1

      - log: "Sending a request with invalid 'price' data type"
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalid_price"
          expect:
            - statusCode: 405
            - hasProperty: "message"
