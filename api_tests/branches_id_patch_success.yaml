# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    headers:
      'api-version': '1.0.0'
    statusCode:
      - 200
  security:
    ApiKeyAuth:
      name: Token
      in: header
      value: {{ $processEnvironment.AUTH_TOKEN }}
payload:
  branches_id_patch_success.csv:
    skipHeader: true
    columns:
      - id
      - tradeName
      - email
      - phone
scenario:
  - get:
      - method: GET
        url: /recipients/branches/{{ branches_id_patch_success.csv.id }}
        description: 'Get branch details'
        log: 'Get branch details'
      - expect:
          statusCode: 200
          contentType: application/json
          hasProperty:
            - id
            - tradeName
            - email
            - phone
            - cocBranchNumber
            - cocNumber
            - eoriNumber
            - deregistrationDate
            - createdAt
            - updatedAt
          equals:
            tradeName: {{ branches_id_patch_success.csv.tradeName }}
          headerEquals:
            'api-version': 1.0.0
  - think: 1000
  - patch:
      - method: PATCH
        url: /recipients/branches/{{ branches_id_patch_success.csv.id }}
        headers:
          Content-Type: application/json
        description: 'Patch branch details'
        payload:
          tradeName: {{ branches_id_patch_success.csv.tradeName }}
          email: {{ branches_id_patch_success.csv.email }}
          phone: {{ branches_id_patch_success.csv.phone }}
        log: 'Patch branch details'
      - expect:
          statusCode: 200
          contentType: application/json
          hasProperty:
            - id
            - tradeName
            - email
            - phone
            - cocBranchNumber
            - cocNumber
            - eoriNumber
            - deregistrationDate
            - createdAt
            - updatedAt
          equals:
            tradeName: {{ branches_id_patch_success.csv.tradeName }}
          headerEquals:
            'api-version': 1.0.0
