# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=6dd436357d

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  payload:
    csv: me_patch_others.csv
    skipHeader: true
scenarios:
  - scenario:
      - description: >-
        Patch with Invalid Auth Token
      - method: PATCH
        url: /me
        security:
          ApiKeyAuth:
            Token: {{$processEnvironment.AUTH_TOKEN}}
      - payload:
          id: 1
          name: "John Doe"
        think: 5000
      - description: >-
        Patch with Invalid JSON Payload
      - method: PATCH
        url: /me
        security:
          ApiKeyAuth:
            Token: {{$processEnvironment.AUTH_TOKEN}}
      - payload:
          id: 1
          name: 123
        expect:
          contentType: application/json
          statusCode: 400
          hasProperty:
            - description
            - error
          notHasProperty:
            - schema_field
            - value
          field: name
        think: 5000
      - description: >-
        Patch with Missing Required Field
      - method: PATCH
        url: /me
        security:
          ApiKeyAuth:
            Token: {{$processEnvironment.AUTH_TOKEN}}
      - payload:
          id: 1
        expect:
          contentType: application/json
          statusCode: 400
          hasProperty:
            - description
            - error
          notHasProperty:
            - schema_field
            - value
          field: name
        think: 5000
      - description: >-
        Patch with Invalid URL
      - method: PATCH
        url: /me/invalid-url
        security:
          ApiKeyAuth:
            Token: {{$processEnvironment.AUTH_TOKEN}}
      - payload:
          id: 1
          name: "John Doe"
        expect:
          statusCode: notStatusCode: [200, 201]
        think: 5000
