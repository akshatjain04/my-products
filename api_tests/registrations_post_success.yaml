# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=16a6858a84


# Artillery test script for successful API requests
config:
  target: {{ $processEnvironment.API_HOST }}
  security:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
      value: {{ $processEnvironment.AUTH_TOKEN }}
  expect: true

payload:
  csv: registrations_post_success.csv
  skipHeader: true

scenarios:
  - flow:
      - get:
          url: "/recipients/branches/{{ branchId }}/registrations"
          headers:
            Accept: application/json
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - id
              - reference
              - credentialUrl
              - startDate
              - endDate
              - status
              - suspendedUntil
              - issuedAt
              - createdAt
              - updatedAt

      - post:
          url: "/recipients/branches/{{ branchId }}/registrations"
          headers:
            Accept: application/json
            Content-Type: application/json
          body: {{ payload }}
          expect:
            statusCode: 201
            contentType: application/json
            hasHeader:
              - api-version
              - location
            hasProperty:
              - id
              - reference
              - credentialUrl
              - startDate
              - endDate
              - status
              - suspendedUntil
              - issuedAt
              - createdAt
              - updatedAt

      - patch:
          url: "/recipients/branches/{{ branchId }}/registrations/{{ id }}"
          headers:
            Accept: application/json
            Content-Type: application/json
          body: {{ payload }}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - id
              - reference
              - credentialUrl
              - startDate
              - endDate
              - status
              - suspendedUntil
              - issuedAt
              - createdAt
              - updatedAt

      - delete:
          url: "/recipients/branches/{{ branchId }}/registrations/{{ id }}"
          headers:
            Accept: application/json
          expect:
            statusCode: 204

      - think: 5000 # 5 seconds
