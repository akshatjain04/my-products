# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  plugins:
    expect: {}
  http:
    pool: 10

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/1234/5678"
          json:
            accountCode: 1234
            profilePin: 5678
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer token"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - equals:
                - "{{ response.body.message }}"
                - "Success"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/1234/5678"
          json:
            accountCode: 1234
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer token"
          expect:
            - statusCode: 400
            - equals:
                - "{{ response.body.message }}"
                - "Validation error: Missing required field profilePin"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/1234/5678"
          json:
            accountCode: "1234@#"
            profilePin: "5678@#"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer token"
          expect:
            - statusCode: 500
            - equals:
                - "{{ response.body.message }}"
                - "Internal server error"

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/1234/5678"
          qs:
            accountCode: 1234
            profilePin: 5678
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer token"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - equals:
                - "{{ response.body.message }}"
                - "Success"
                
  - name: "API request with unauthorized access"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/1234/5678"
          json:
            accountCode: 1234
            profilePin: 5678
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer wrong_token"
          expect:
            - statusCode: 401
            - equals:
                - "{{ response.body.message }}"
                - "Unauthorized access"
