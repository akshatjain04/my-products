import csv
import random
import faker
from faker import Faker

# Initialize faker generator
fake = Faker()

# Define the headers/variables
headers = ['id', 'first_name', 'last_name', 'email', 'date_of_birth', 'phone_number']

# Generate dummy data
data = []
for _ in range(2):
    id = random.randint(1, 100)
    first_name = fake.first_name()
    last_name = fake.last_name()
    email = fake.email()
    date_of_birth = fake.date_of_birth(minimum_age=18, maximum_age=90)
    phone_number = fake.phone_number()
    data.append([id, first_name, last_name, email, date_of_birth, phone_number])

# Write data to CSV file
with open('dummy_data.csv', 'w', newline='') as file:
    writer = csv.writer(file)
    writer.writerow(headers)
    writer.writerows(data)
