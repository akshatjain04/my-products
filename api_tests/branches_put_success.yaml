# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/branches_put for http method type PUT in artillery framework

# RoostTestHash=63322ffb26


# Artillery test script for API testing
config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true
  securitySchemes:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'

scenarios:
  # Scenario 1: Create a new branch with valid payload
  - name: Create Branch with Valid Payload
    flow:
      - get:
          url: /recipients/branches
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty: id
            hasProperty: tradeName
            hasProperty: cocBranchNumber
      - think: 500ms
      - post:
          url: /recipients/branches
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
            Content-Type: application/json
          payload:
            $csv: branches_put_success.csv
            skipHeader: true
          expect:
            statusCode: 201
            contentType: application/json
            hasProperty: id
            hasProperty: tradeName
            hasProperty: cocBranchNumber
            hasHeader: api-version
            headerEquals: api-version: 1.0.0
      - think: 500ms
