# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - log: "Executing successful API request scenario"
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin"
          headers:
            Content-Type: "application/json"
          capture:
            - json: "$"
              as: "response"
      - think: 5
      - assert:
          - statusCode: 200
          - bodyContains:
            - "{{ response }}"
  - name: "API request with validation failure"
    flow:
      - log: "Executing API request with validation failure scenario"
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode"
          headers:
            Content-Type: "application/json"
          capture:
            - json: "$"
              as: "response"
      - think: 5
      - assert:
          - statusCode: 400
          - bodyContains:
            - "{{ response }}"
  - name: "API request with edge case"
    flow:
      - log: "Executing API request with edge case scenario"
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin?specialCharacters=%$#"
          headers:
            Content-Type: "application/json"
          capture:
            - json: "$"
              as: "response"
      - think: 5
      - assert:
          - statusCode: 200
          - bodyContains:
            - "{{ response }}"
  - name: "API request with query parameters"
    flow:
      - log: "Executing API request with query parameters scenario"
      - get:
          url: "/certainExternal/service/v1/Profile/:accountCode/:profilePin?param1=value1&param2=value2"
          headers:
            Content-Type: "application/json"
          capture:
            - json: "$"
              as: "response"
      - think: 5
      - assert:
          - statusCode: 200
          - bodyContains:
            - "{{ response }}"
