# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=64d33906e5

config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true

scenarios:
  - scenario: Failure Scenario
    flow:
      - get:
          url: /logout
          description: "GET /logout without authentication (expect 401)"
          expect:
            - statusCode: 401
            - hasHeader: www-authenticate
          log: "GET /logout without authentication (expect 401)"
          think: 500

      - post:
          url: /logout
          description: "POST /logout with invalid payload (expect 400)"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            incorrect_field: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
          log: "POST /logout with invalid payload (expect 400)"
          think: 500

      - post:
          url: /logout
          description: "POST /logout with missing mandatory field (expect 400)"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            token: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
          log: "POST /logout with missing mandatory field (expect 400)"
          think: 500

      - post:
          url: /logout
          description: "POST /logout with invalid URL (expect 404)"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            - statusCode: 404
          log: "POST /logout with invalid URL (expect 404)"
          think: 500
