# Test generated by RoostGPT for test MiniProjects using AI Type Vertex AI and AI Model code-bison-32k


# Integration Test Scenarios

--- Scenario Start ---
scenario: Create a new product
  # Given
  - let payload = {
      name: "Test Product",
      description: "This is a test product.",
      price: 10.0,
    };
  - let headers = {
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}",
    };

  # When
  - let response = post("/api/products", payload, headers);

  # Then
  - expect(response.status).to.equal(201);
  - expect(response.body).to.have.property("id");
  - expect(response.body).to.have.property("name").that.equals(payload.name);
  - expect(response.body).to.have.property("description").that.equals(payload.description);
  - expect(response.body).to.have.property("price").that.equals(payload.price);
  - expect(response.body).to.have.property("createdAt");
  - expect(response.body).to.have.property("updatedAt");
  - expect(response.body).to.have.property("v");
--- Scenario End ---
