# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview


config:
  target: "{{ $processEnvironment.API_HOST }}/api"
  plugins:
    expect: {}

scenarios:
  - name: "Create a new product"
    flow:
      - post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "Test Product"
            description: "This is a test product."
            price: 10.0
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - equals:
                name: "Test Product"
                description: "This is a test product."
                price: 10.0
          log: "Creating a new product with valid payload should succeed and return the product details."
      - think: 1
