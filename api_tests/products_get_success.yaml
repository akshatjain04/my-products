# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /products_get for http method type GET in artillery framework

# RoostTestHash=70199667a6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get All Products"
    flow:
      - log: "Sending request to get all products"
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - log: "Validating the response contains the required name property"
      - expect:
          - statusCode: 200
          - hasProperty: "name"
          - hasHeader: "Content-Type"
