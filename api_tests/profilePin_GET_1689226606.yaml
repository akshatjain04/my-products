# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    fields:
      - "accountCode"
      - "profilePin"
    values:
      - ["testAccount1", "testProfile1"]
      - ["testAccount2", "testProfile2"]
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ $processEnvironment.accountCode }}/{{ $processEnvironment.profilePin }}"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ $processEnvironment.accountCode }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ $processEnvironment.accountCode }}/{{ $processEnvironment.profilePin }}"
          expect:
            - statusCode: "{{ $processEnvironment.statusCode }}"
            - hasProperty: "profileId"
