# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /cancelProductOrder/{order.Id}_post for http method type POST in artillery framework
# RoostTestHash=284cdcff34
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload:
  - path: "cancelProductOrder_order.Id_post_success.csv"
    fields:
      - "order.Id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
  environments:
    target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Cancel Product Order By Id"
    flow:
      - log: "Starting test for cancelling product order by Id API"
      
      - post:
          url: "/cancelProductOrder/{{ order.Id }}"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            order:
              associatedDocument: "VTT001"
              customer:
                subscriberId: "SVTAL001"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - hasProperty: "type"
            - hasProperty: "code"
      - think: 1

      - log: "Test for cancelling product order by Id API finished successfully"
