# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# RoostTestHash=74a40da00e

---
title: Non-Success API Response Test Script
config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true
security_schema:
  ApiKeyAuth:
    type: 'apiKey'
    name: 'Token'
    in: 'header'
payload:
  branches_get_others.csv:
    skipHeader: true
scenario:
  - get:
      path: /recipients/branches
      description: Get branches without any parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789
      description: Get branches with invalid coc-number (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-branch-number=12345678901234
      description: Get branches with invalid coc-branch-number (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22
      description: Get branches with invalid trade-name (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?size=51
      description: Get branches with size exceeding limit (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?offset=-1
      description: Get branches with negative offset (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?size=a
      description: Get branches with invalid size (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?offset=a
      description: Get branches with invalid offset (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?trade-name=
      description: Get branches with empty trade-name (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=51&offset=-1
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=a&offset=a
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=51&offset=50
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=20&offset=-1
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=20&offset=50
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=a&offset=50
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=51&offset=a
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
  - get:
      path: /recipients/branches?coc-number=123456789&trade-name=%22test%22&size=a&offset=a
      description: Get branches with multiple invalid parameters (should return 400)
      expect:
        statusCode: 400
