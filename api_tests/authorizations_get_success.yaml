# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: {{ $processEnvironment.API_HOST }}
  expect: enable
  security:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
      value: {{$processEnvironment.AUTH_TOKEN}}
  payload:
    authorizations_get_success.csv:
      skipHeader: true
scenarios:
  - flow:
      # GET /authorizations
      - get:
          url: "/authorizations"
          headers:
            Authorization: ApiKeyAuth {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasHeader: api-version
            hasHeader: content-type
            headerEquals: content-type: application/json
            hasProperty: total
            hasProperty: authorizations
            authorizations:
              - hasProperty: id
              - hasProperty: name
              - hasProperty: email
              - hasProperty: createdAt
              - hasProperty: updatedAt
      - think: 500
