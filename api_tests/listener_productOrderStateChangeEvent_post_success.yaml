# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /listener/productOrderStateChangeEvent_post for http method type POST in artillery framework
# RoostTestHash=6f5fd9fdba
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  payloads:
    - path: "listener_productOrderStateChangeEvent_post_success.csv"
      fields: ["date", "subscriberId", "id", "correlationOrder", "associatedDocument", "code", "description", "lostEquipment", "gponAccess", "fiberPath"]
      skipHeader: true
      delimiter: "^|^"
  plugins:
    expect: {}
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: 'Test API endpoint /listener/productOrderStateChangeEvent'
    flow:
      - log: 'Executing POST request to /listener/productOrderStateChangeEvent'
      - post:
          url: '/listener/productOrderStateChangeEvent'
          json:
            notification:
              date: "{{ date }}"
              customer:
                subscriberId: "{{ subscriberId }}"
              order:
                id: "{{ id }}"
                correlationOrder: "{{ correlationOrder }}"
                associatedDocument: "{{ associatedDocument }}"
                code: "{{ code }}"
                description: "{{ description }}"
                lostEquipment: "{{ lostEquipment }}"
                resource:
                  gponAccess: "{{ gponAccess }}"
                  fiberPath: "{{ fiberPath }}"
          headers:
            AUTH_TOKEN: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 204
      - think: 1
