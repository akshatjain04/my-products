# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}/certainExternal/service/v1/Profile/"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_GET_1689669665.csv"
    fields:
      - "accountCode"
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ accountCode }}"
          expect:
            - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidAccountCode"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ accountCode }}"
          expect:
            - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/{{ accountCode }}?queryParam=value"
          expect:
            - statusCode: 200

  - name: "API request with authorization"
    flow:
      - get:
          url: "/{{ accountCode }}"
          expect:
            - statusCode: 200
