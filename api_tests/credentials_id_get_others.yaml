# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Non-successful API calls to /credentials/{id} with invalid id"
    flow:
      - log: "Attempting to GET /credentials/{id} with an invalid id"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_AUTH_TOKEN }}"
          capture:
            json: "$.invalidUUID"
            as: "invalidUUID"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - think: 5
      - log: "Attempting to GET /credentials/{id} without an id"
      - get:
          url: "/credentials/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
      - log: "Attempting to GET /invalidcredentials/{id}"
      - get:
          url: "/invalidcredentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.url1_credentialregister-wallet-1.0.0-resolved_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
