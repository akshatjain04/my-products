# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.CREDENTIAL_WALLET_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account"
    flow:
      - log: "Starting test for POST /accounts to create a new account"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{ $processEnvironment.COMMIT_HASH }}"
            Authorization: "Bearer {{ $processEnvironment.CREDENTIAL_WALLET_AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasHeader: "commit-hash"
            - matchesRegexp: {"commit-hash": "^[a-z0-9]{7}$"}
      - think: 1
