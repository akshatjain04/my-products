# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  variables:
    commitHash: "928d28d"
  plugins:
    expect: {}

scenarios:
  - flow:
      - log: "Starting the scenario for creating a new account"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{ commitHash }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals: { api-version: "1.0.0" }
            - hasHeader: "commit-hash"
            - headerEquals: { commit-hash: "{{ commitHash }}" }
          capture:
            json: "$.email"
            as: "createdEmail"
      - think: 1
