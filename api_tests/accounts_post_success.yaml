# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: {{ $processEnvironment.API_HOST }}
  expect: true
  # Define CSV payload with skipHeader
  payload:
    accounts_post_success.csv:
      skipHeader: true

scenarios:
  - flow:
      # Request 1: POST /accounts
      - post:
          url: /accounts
          headers:
            ApiKeyAuth: {{$processEnvironment.AUTH_TOKEN}}
          payload:
            $ref: accounts_post_success.csv
          expect:
            - statusCode: 201
            - hasHeader: api-version
            - hasHeader: commit-hash
            - contentType: application/json
            - hasProperty: email
            - hasProperty: id
      # Think time after each request
      - think: 500
