# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "http://api.test-target.com"  # Changed to a placeholder URL since environment variables are not evaluated here
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_success.csv"  # Correct filename for payload
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"  # Correct delimiter added

scenarios:
  - name: "Create new account and validate response"
    flow:
      - log: "Starting the create account scenario"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "{{ commitHash }}"
            Authorization: "Bearer hardcoded_auth_token"  # Changed to a hardcoded token for compliance
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals:
                header: "api-version"
                value: "1.0.0"
            - matchesRegexp:
                header: "commit-hash"
                regexp: "^[a-z0-9]{7}$"
            - hasProperty: "email"
            - matchesRegexp:
                property: "email"
                regexp: "^[^<>()[\\]\\\\.,;:\\s@\"]+@[^<>()[\\]\\\\.,;:\\s@\"]+\\.[a-zA-Z]{2,}$"
      - think: 1
