# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "api_GET_1690354568.csv"
    fields:
      - "id"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/api/products/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasProperty: "category"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/api/products/invalid-id"
          expect:
            - statusCode: 400
            - hasProperty: "message"
  - name: "API request with edge case"
    flow:
      - get:
          url: "/api/products/very-long-id-with-special-characters-!@#$%^&*()"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasProperty: "category"
  - name: "API request with query parameters"
    flow:
      - get:
          url: "/api/products/{{ id }}?sort=price&filter=electronics"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasProperty: "category"
  - name: "API request with authorization"
    flow:
      - get:
          url: "/api/products/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasProperty: "category"
