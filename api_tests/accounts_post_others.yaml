# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.CREDENTIAL_WALLET_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_others.csv"
    skipHeader: true
    fields:
      - "email"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "API Failure Tests"
    flow:
      - log: "Testing with invalid email format"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
          json:
            email: "invalid-email-format"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing with missing email field"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing with missing commit-hash header"
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing with invalid commit-hash header format"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "12345678"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing with an invalid URL"
      - post:
          url: "/invalid-url"
          headers:
            commit-hash: "abcdefg"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
