# ********RoostGPT********

# Test generated by RoostGPT for test MiniProjects using AI Type  and AI Model 
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.CREDENTIAL_WALLET_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "API Failure Tests"
    flow:
      - log: "Testing with invalid email format"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
            Content-Type: "application/json"
          json:
            email: "invalid_email_format"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing without commit-hash header"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Testing with invalid URL"
      - post:
          url: "/invalid_endpoint"
          headers:
            commit-hash: "abcdefg"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Testing with missing email in payload"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
            Content-Type: "application/json"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Testing with unauthorized access"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
