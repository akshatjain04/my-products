# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "data.csv"
    fields:
      - "accountCode"
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            # Add more assertions here based on the expected response body

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/INVALID_ACCOUNT_CODE"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            # Add more assertions here based on the expected error message

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ edgeCaseAccountCode }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ expectedStatusCode }}"
            # Add more assertions here based on the expected response body

  - name: "API request with query parameters"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}?queryParam=value"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            # Add more assertions here based on the expected response body

  - name: "API request with authorization"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.authToken }}"
          expect:
            - statusCode: 200
            # Add more assertions here based on the expected response body for an authorized request
