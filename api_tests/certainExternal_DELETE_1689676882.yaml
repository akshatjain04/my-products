# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_DELETE_1689676882.csv"
    fields:
      - "accountCode"
      - "profilePin"
  http:
    auth:
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - delete: "/certainExternal/service/v1/Profile/invalid_account_code/invalid_profile_pin"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete: "/certainExternal/service/v1/Profile/1234567890123456/1234567890123456"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}?param1=value1&param2=value2"
        headers:
          Content-Type: "application/json"
        expect:
          - statusCode: 200

  - name: "API request with authorization"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        headers:
          Content-Type: "application/json"
          Authorization: "Bearer {{ $processEnvironment.authToken }}"
        expect:
          - statusCode: 200
