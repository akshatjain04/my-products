# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /recipients/persons/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=1416aa2295


# Artillery test script for API endpoint: /recipients/persons/{id}
config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true

scenarios:
  - flow:
      - get:
          # GET /recipients/persons/{id}
          # Retrieve a Person
          url: /recipients/persons/{{ row.id }}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - id
              - initials
              - firstName
              - middleName
              - lastName
              - birthDate
              - birthPlace
              - phone
              - email
              - postalCode
              - houseNumber
              - houseNumberPostfix
              - countryCode
              - reference
              - createdAt
              - updatedAt
      - think: 500
      - post:
          # POST /recipients/persons/{id}
          # Update a Person
          url: /recipients/persons/{{ row.id }}
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          json:
            initials: "{{ row.initials }}"
            firstName: "{{ row.firstName }}"
            middleName: "{{ row.middleName }}"
            lastName: "{{ row.lastName }}"
            birthDate: "{{ row.birthDate }}"
            birthPlace: "{{ row.birthPlace }}"
            phone: "{{ row.phone }}"
            email: "{{ row.email }}"
            postalCode: "{{ row.postalCode }}"
            houseNumber: "{{ row.houseNumber }}"
            houseNumberPostfix: "{{ row.houseNumberPostfix }}"
            countryCode: "{{ row.countryCode }}"
            reference: "{{ row.reference }}"
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - id
              - initials
              - firstName
              - middleName
              - lastName
              - birthDate
              - birthPlace
              - phone
              - email
              - postalCode
              - houseNumber
              - houseNumberPostfix
              - countryCode
              - reference
              - createdAt
              - updatedAt
      - think: 500
      - patch:
          # PATCH /recipients/persons/{id}
          # Update a Person
          url: /recipients/persons/{{ row.id }}
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          json:
            initials: "{{ row.initials }}"
            firstName: "{{ row.firstName }}"
            middleName: "{{ row.middleName }}"
            lastName: "{{ row.lastName }}"
            birthDate: "{{ row.birthDate }}"
            birthPlace: "{{ row.birthPlace }}"
            phone: "{{ row.phone }}"
            email: "{{ row.email }}"
            postalCode: "{{ row.postalCode }}"
            houseNumber: "{{ row.houseNumber }}"
            houseNumberPostfix: "{{ row.houseNumberPostfix }}"
            countryCode: "{{ row.countryCode }}"
            reference: "{{ row.reference }}"
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              - id
              - initials
              - firstName
              - middleName
              - lastName
              - birthDate
              - birthPlace
              - phone
              - email
              - postalCode
              - houseNumber
              - houseNumberPostfix
              - countryCode
              - reference
              - createdAt
              - updatedAt
      - think: 500
      - delete:
          # DELETE /recipients/persons/{id}
          # Delete a Person
          url: /recipients/persons/{{ row.id }}
          headers:
            Token: {{ $processEnvironment.AUTH_TOKEN }}
          expect:
            statusCode: 204
      - think: 500

payload:
  file: persons_id_patch_success.csv
  skipHeader: true
