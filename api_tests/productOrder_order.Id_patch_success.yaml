# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /productOrder/{order.Id}_patch for http method type PATCH in artillery framework
# RoostTestHash=baabcd7d73
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "productOrder_order.Id_patch_success.csv"
    fields:
      - "correlationOrder"
      - "associatedDocument"
      - "associatedDocumentDate"
      - "type"
      - "infraType"
      - "addresses"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.host"
  plugins:
    expect: {}

scenarios:
  - name: "Patch Product Order By Id"
    flow:
      - log: "Starting patch"
      - patch:
          url: "/productOrder/{{ correlationOrder }}"
          json: 
            associatedDocument: "{{ associatedDocument }}"
            associatedDocumentDate: "{{ associatedDocumentDate }}"
            type: "{{ type }}"
            infraType: "{{ infraType }}"
            addresses: "{{ addresses }}"
          headers:
            your-security-scheme-name: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - hasProperty: "type"
            - hasProperty: "code"
      - think: 1
