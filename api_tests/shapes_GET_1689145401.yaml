# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/v3/shapes"
          json:
            key1: "value1"
            key2: "value2"
          headers:
            Authorization: "Bearer token"
          expect:
            - statusCode: 200
            - hasProperty: "data"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/v3/shapes"
          json:
            key1: "value1"
          headers:
            Authorization: "Bearer token"
          expect:
            - statusCode: 400
            - hasProperty: "error"
  - name: "API request with edge case"
    flow:
      - post:
          url: "/v3/shapes"
          json:
            key1: "value1"
            key2: "value2"
            key3: "very very very long value"
          headers:
            Authorization: "Bearer token"
          expect:
            - statusCode: 200
            - hasProperty: "data"
