# Test generated by RoostGPT for test credential-wallet-api-akshat using AI Type Vertex AI and AI Model code-bison-32k


# Test generated for /me_patch for http method type PATCH in artillery framework

# RoostTestHash=6dd436357d


config:
  target: {{ $processEnvironment.API_HOST }}
  expect:
    enabled: true
  securitySchemes:
    ApiKeyAuth:
      type: 'apiKey'
      name: 'Token'
      in: 'header'
  payload:
    me_patch_success.csv:
      skipHeader: true
      fields:
        - name
        - image
        - settings
scenarios:
  - flow:
      - get:
          url: "/me"
          authorization:
            bearer: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              id:
                type: string
              did:
                type: string
              name:
                type: string
              image:
                type: string
              email:
                type: string
              settings:
                type: object
              createdAt:
                type: string
              updatedAt:
                type: string
      - think: 500
      - patch:
          url: "/me"
          authorization:
            bearer: {{$processEnvironment.AUTH_TOKEN}}
          headers:
            Content-Type: application/json
          payload:
            $ref: "me_patch_success.csv"
          expect:
            statusCode: 200
            contentType: application/json
            hasProperty:
              id:
                type: string
              did:
                type: string
              name:
                type: string
              image:
                type: string
              email:
                type: string
              settings:
                type: object
              createdAt:
                type: string
              updatedAt:
                type: string
