# ********RoostGPT********

# Test generated by RoostGPT for test ProductOrdering-restAssured-API-Spec-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /cancelProductOrder/{order.Id}_post for http method type POST in artillery framework
# RoostTestHash=284cdcff34
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "cancelProductOrder_order.Id_post_others.csv"
    fields:
      - "order.Id"
      - "order.associatedDocument"
      - "order.customer.subscriberId"
    skipHeader: true
    delimiter: "^|^"
  http:
    pool: 10

scenarios:
  - name: "Scenario for unsuccessful API requests"
    flow:
      - log: "Initiating POST request with incorrect order.Id"
      - post:
          url: "/cancelProductOrder/{{ order.Id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            order:
              associatedDocument: "{{ order.associatedDocument }}"
              customer:
                subscriberId: "{{ order.customer.subscriberId }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 5

      - log: "Initiating POST request with missing order associated document"
      - post:
          url: "/cancelProductOrder/{{ order.Id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            order:
              associatedDocument: ""
              customer:
                subscriberId: "{{ order.customer.subscriberId }}"
          expect:
            - statusCode: 422
            - hasProperty: "error"
      - think: 5

      - log: "Initiating POST request with missing order.customer.subscriberId"
      - post:
          url: "/cancelProductOrder/{{ order.Id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            order:
              associatedDocument: "{{ order.associatedDocument }}"
              customer:
                subscriberId: ""
          expect:
            - statusCode: 422
      - think: 5

      - log: "Initiating POST request with incorrect url"
      - post:
          url: "/cancelProductInvalid/{{ order.Id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            order:
              associatedDocument: "{{ order.associatedDocument }}"
              customer:
                subscriberId: "{{ order.customer.subscriberId }}"
          expect:
            - statusCode: 404
      - think: 5
