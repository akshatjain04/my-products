# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://{{ $processEnvironment.hostname }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_DELETE_168966740.csv"
    fields:
      - "accountCode"
      - "profilePin"
  defaults:
    headers:
      Content-Type: "application/json"
    auth:
      mode: "basic"
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        expect:
          - statusCode: 200

  - name: "API request with validation failure"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        expect:
          - statusCode: 200

  - name: "API request with query parameters"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        expect:
          - statusCode: 200

  - name: "API request with authorization"
    flow:
      - delete: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
        expect:
          - statusCode: 200
